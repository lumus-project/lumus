import { element } from '@lumus/engine';

const AppLayout = element('div', {
    name: 'LumusAppLayout',
    classNames: {
        variants: {
            inline: 'inline',
        },
    },
});

export default AppLayout;
