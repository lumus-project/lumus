import { element } from '@lumus/engine';

const Box = element('div', {
    name: 'LumusHeader',
    classNames: {
        base: '',
        variants: {
            inline: 'inline',
        },
    },
});

export default Box;
